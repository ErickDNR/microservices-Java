server:
  port: 8888
spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ErickDNR/microservices-java
          search-paths:
          - microservices-java/configs/{application}
          #username: ErickDeNardi
          #password: senha